# Backend — KirtiBuildWell

Express + TypeScript backend.

Dev:

```
cd backend
npm install
cp .env.example .env
npm run dev
```

Endpoints (stubs):
- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/properties`
- `POST /api/properties`
- `POST /api/leads`
